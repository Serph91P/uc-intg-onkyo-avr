## Log level

As from v0.8.10+ the default log level is `warn`. When you run setup you can reconfigure the integration to do a lot more logging. This can be handy in case of debugging for example.

During normal operation, `warn` level is the preferred setting to lower the cpu/power consumption a wee bit while still being able to log when really needed.
